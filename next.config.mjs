/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "drive.google.com",
      "up4u.ir",
      "ibb.co",
      "kafechenar.s3.ir-thr-at1.arvanstorage.ir",
    ],
  },
};

export default nextConfig;
